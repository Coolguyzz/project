doctype html
html
    head
        title= "socket test 1 "
        script(src='/socket.io/socket.io.js')
        script(src='/chat.js')

    body
        content(id='content' style='width: 500px; height: 300px; margin: 0 0 20px 0; border: solid 1px #999; overflow-y: scroll')
        .controls
            input.field(id='field', style='width:350px;')
            input.send(type='button', value='send',id='send')
